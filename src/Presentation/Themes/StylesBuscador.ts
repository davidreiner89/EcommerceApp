import {StyleSheet} from 'react-native';

export const StylesBuscador = StyleSheet.create({
  container: {
    position: 'relative',
    width: '100%',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 16,
    marginVertical: 13,
  },
});
