import {StyleSheet} from 'react-native';

export const StylesRutas = StyleSheet.create({
  iconContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#ffffff',
    borderRadius: 999,
    padding: 6,
    elevation: 2,
  },
});
